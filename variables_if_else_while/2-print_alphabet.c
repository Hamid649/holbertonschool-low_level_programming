#include <stdio.h>

/**
 * main - This is the entry point of the code
 *
 * Return: 0 Successful
 */
int main(void)
{
    char c;

    for (c = 'a'; c <= 'z'; c++)
        putchar(c);
    putchar('\n');

    return (0);
}

